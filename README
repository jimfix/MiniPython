Copyright (C) 2015 Jeremy Cosel & James Fix

This directory contains a scanner and parser for the Python programming language.

You need JFlex, the lexical analyzer, and CUP, the parser generator, for this to compile.

Set up by typing:

jflex unicode.flex
jflex minipython.flex
java java_cup.Main -interface < minipython.cup
javac *.java


The parser can be tested with:
java TestParser <inputfiles>

The scanner (without parser attached) can be test with:
java TestLexer <inputfiles>


files:

unicode.flex
  JFlex specification for the Unicode preprocessing phase
  (see section 3.3 of the Java Language Specification).
  Demonstrates how to implement a FilterReader with JFlex.

minipython.flex
  JFlex specification for the "MiniPython" lexer.

minipython.cup
  CUP specification for the "MiniPython" parser
  
TestParser.java
  a simple main class to run the parser (no other useful output though)

TestLexer.java
  a simple test driver for the scanner, produces debug output

lexer-output.good
  lexer output for input file TestLexer.java for testing